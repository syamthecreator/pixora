import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const black = Color(0xFF0A0A0F);
  static const Color kAccentYellow = Color(0xFFFFC857);
  
  // Quick Settings Sheet
  static const Color sheetBackground = Color(0xFF0A0A0F);
  static const Color activeOption = Color(0xFFF5C542);
  static const Color inactiveOption = Color(0xFF1A1A22);
  
  // Optional: Add white for consistency
  static const Color white = Colors.white;

    static const Color primaryGreen = Color(0xFF16A34A);
  static const Color primaryTeal = Color(0xFF0F766E);
  static const Color primaryLime = Color(0xFF65A30D);

  // 🧠 Text
  static const Color title = Color(0xFF0F172A);
  static const Color subtitle = Color(0xFF64748B);

  // 🎨 Background
  static const Color backgroundLight = Color(0xFFF8FAFC);
}
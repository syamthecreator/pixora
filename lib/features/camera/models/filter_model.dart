class FilterModel {
  final String key;   // ENUM KEY
  final String name;  // DISPLAY NAME
  final String image;

  FilterModel({
    required this.key,
    required this.name,
    required this.image,
  });
}
